# Source Artifacts

This folder stores extracted or downloaded source bundles for reference.

These files are not treated as app data and should not be imported directly into React pages. Use them to recover formulas, mappings, or UI behavior, then move cleaned data or code into the appropriate `src/data/*` or `src/utils/*` module.
